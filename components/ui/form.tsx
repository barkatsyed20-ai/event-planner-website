import * as React from "react";

export function Form({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}

export function FormField({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}